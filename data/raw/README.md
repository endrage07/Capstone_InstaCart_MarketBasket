# Raw Data

This directory is intended to store the raw Instacart CSV files.

Raw data files are not committed to version control due to size constraints.
The dataset is loaded dynamically via KaggleHub in the notebooks.
